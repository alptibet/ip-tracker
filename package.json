{
  "name": "ip-tracker",
  "version": "1.0.0",
  "description": "IP address tracking application",
  "main": "index.html",
  "scripts": {
    "devserver": "live-server --no-browser",
    "watch:sass": "sass --no-source-map --watch src/scss/main.scss:style.css",
    "start": "npm-run-all --parallel watch:sass devserver"
  },
  "author": "Alp Tibet",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.32.13"
  },
  "dependencies": {
    "leaflet": "^1.7.1",
    "npm-run-all": "^4.1.5"
  }
}
